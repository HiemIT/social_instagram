import 'package:flutter/material.dart';

class ItemUserWidget extends StatelessWidget {
  const ItemUserWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
