import 'package:flutter/material.dart';

class CommentItemBubble extends StatelessWidget {
  const CommentItemBubble({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
